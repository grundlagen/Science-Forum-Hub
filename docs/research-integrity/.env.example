# Postgres connection used by @workspace/db (drizzle).
DATABASE_URL=postgres://user:pass@localhost:5432/science_forum_hub

# OpenAlex polite-pool contact email (optional but recommended).
OPENALEX_MAILTO=

# NCBI / PubMed E-utilities API key (optional; raises rate limit). Used in M2.
NCBI_API_KEY=
