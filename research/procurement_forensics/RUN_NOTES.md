# Run notes

The local analysis environment used to develop this branch cannot resolve arbitrary public internet hosts, so corpus acquisition is intentionally executed in GitHub Actions. This keeps public-source downloads reproducible and captures the exact code/version used for each screen.

The workflow output is lead-generation only. Candidate pairs should be manually falsified before further investigation.
